---
title: 🧩 Composite
id: composite
---

## Overview

Renders multiple child models **layered on top of each other** in the same space. Rendered from last to first — earlier entries appear on top.

```yaml
items:
  demo:gem:
    material: diamond
    model:
      type: minecraft:composite
      models:
        - type: minecraft:model
          path: minecraft:item/custom/gem_glow   # Top layer
        - type: minecraft:model
          path: minecraft:item/custom/gem_base   # Bottom layer
```

## Fields

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `type` | string | Yes | — | `minecraft:composite` |
| `models` | list | Yes | — | List of child item models to render in order |
| `transformation` | object | No | — | Display transform overlay, composed with child transforms (26.1+) |


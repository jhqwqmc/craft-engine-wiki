---
title: ∅ Empty
id: empty
---

## Overview

Renders **nothing**. Useful as a fallback for selection models when you want certain property values to show no item model.

```yaml
items:
  demo:hidden_item:
    material: paper
    model:
      type: minecraft:empty
```

`transformation` is **not** supported on `empty`.

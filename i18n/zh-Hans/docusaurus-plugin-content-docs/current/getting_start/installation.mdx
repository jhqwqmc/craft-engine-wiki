---
title: 📦 安装与托管
id: installation
---

import Highlight from '@site/src/components/Highlight';
import ColoredLink from '@site/src/components/ColoredLink';
import UrlCard from '@site/src/components/UrlCard';
import PluginFileTree from '@site/src/components/PluginFileTree';

把插件跑起来，让资源包能发给玩家。

## 要求

- Paper 系服务端，已验证以下：

**Paper 及其下游**：Paper、Purpur、Pufferfish、Leaf、Leaves、UniverseSpigot

**Folia 及其下游**：Folia、Canvas、Luminol

:::info
- **Java：** JDK 21 或更高
- **Minecraft：** 1.20 或更高
:::

## 获取插件

- **社区版**<Highlight color="#00AF5C"><ColoredLink to="https://modrinth.com/plugin/craftengine" color="#ffffff">**Modrinth**</ColoredLink></Highlight>
- **付费版**<Highlight color="#06B6D4"><ColoredLink to="https://voxel.shop/product/7624/craftengine" color="#ffffff">**VoxelShop**</ColoredLink></Highlight>、<Highlight color="#2563EB"><ColoredLink to="https://builtbybit.com/resources/craftengine.82674/" color="#ffffff">**BuiltByBit**</ColoredLink></Highlight>、<Highlight color="#3B0764"><ColoredLink to="https://nmcrate.com/products/craftengine-1" color="#ffffff">**NMCrate**</ColoredLink></Highlight>、<Highlight color="#7C3AED"><ColoredLink to="https://afdian.com/a/xiaomomi" color="#ffffff">**爱发电**</ColoredLink></Highlight>

## 安装

把 `.jar` 放进 `plugins/`，启动服务器。首次启动会生成：

<PluginFileTree
  title="CraftEngine"
  initialTreeData={[
    { id: "generated", name: "generated", hoverText: "生成好的 resource_pack.zip 生成在这里", children: [] },
    { id: "libs", name: "libs", hoverText: "运行时依赖，按需加载，不用管", children: [] },
    { id: "resources", name: "resources", hoverText: "★ 你的所有创作放在这里。每个子文件夹是一个包。", children: [] },
    { id: "translations", name: "translations", hoverText: "命令反馈与警告的多语言文本", children: [] },
    { id: "commands.yml", name: "commands.yml", isLeaf: true, hoverText: "命令的启用、权限、别名" },
    { id: "config.yml", name: "config.yml", isLeaf: true, hoverText: "主配置：托管方式、性能、功能开关" },
  ]}
/>

<div style={{ marginBottom: "32px" }}></div>

进游戏打 `/ce`，弹出物品浏览器说明安装成功。

## 资源包托管

CraftEngine 把你 `resources/` 下的内容生成成一个资源包，通过 HTTP 发给玩家。

**现在先试试能不能用**：进游戏打 `/ce reload all`。如果资源包下载成功、没有紫黑贴图——说明托管已经生效。这是因为默认配置就是 `self` 模式，服务器自带 HTTP 分发功能，不需要额外设置。

如果下载失败，看下一节。

### 换托管方式

如果默认配置下无法完成资源包的发送，或是你想试试其他方式，可以考虑以下托管方案。如果你想弄明白自托管到底出了什么问题，请进入[自托管页面](set_up_host/self)查看详细配置教程。

<div style={{display: 'flex', flexWrap: 'wrap', margin: '-10px -10px 0 -10px'}}>

<UrlCard url="set_up_host/lobfile" title="📤 Lobfile" subtitle="免费文件托管，零配置"/>
<UrlCard url="set_up_host/s3" title="☁️ S3 / 对象存储" subtitle="Cloudflare R2、腾讯云 COS 等"/>
<UrlCard url="set_up_host/self" title="🖥️ 自托管" subtitle="服务器自己发，免费"/>
<UrlCard url="set_up_host/external" title="🔗 外部托管" subtitle="指向你已有的托管"/>
<UrlCard url="set_up_host/openlist" title="📋 OpenList" subtitle="OpenList 实例分发"/>
<UrlCard url="set_up_host/onedrive" title="📁 OneDrive" subtitle="OneDrive 分发"/>
<UrlCard url="set_up_host/gitlab" title="🦊 Gitlab" subtitle="GitLab 仓库分发"/>
<UrlCard url="set_up_host/dropbox" title="📦 Dropbox" subtitle="Dropbox 分发"/>
<UrlCard title="🚫 None" subtitle="只生成 zip，不上传"/>

</div>

> ⚠️ 改完 `config.yml` 的托管配置后，跑 `/ce reload all`。单跑 `config` 不会重建资源包。

> ⚠️ 需要 API 密钥的托管（如 S3），用**环境变量**传密钥，不要把密钥直接写在 `config.yml` 里。**不要**把含密钥的配置文件发给你不信任的人。

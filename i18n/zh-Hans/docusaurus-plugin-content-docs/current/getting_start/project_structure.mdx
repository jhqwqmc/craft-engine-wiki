---
title: 🗂️ 认识项目
id: project_structure
---

import PluginFileTree from '@site/src/components/PluginFileTree';
import Details from '@site/src/components/Details';

上一章完成了插件安装。本章先认识 CraftEngine 的目录结构，再用一条命令创建教程所需的工作区。

## 插件目录结构

打开 `plugins/CraftEngine/`，可以看到以下内容：

<PluginFileTree
  title="CraftEngine"
  initialTreeData={[
    { id: "generated", name: "generated", hoverText: "生成的 resource_pack.zip 放在这里", children: [] },
    { id: "libs", name: "libs", hoverText: "按需加载的运行时依赖，无需手动管理", children: [] },
    { id: "resources", name: "resources", hoverText: "★ 所有自定义内容都放在这里，每个子文件夹对应一个包", children: [] },
    { id: "translations", name: "translations", hoverText: "命令反馈与警告的多语言文本", children: [] },
    { id: "commands.yml", name: "commands.yml", isLeaf: true, hoverText: "命令的启用、权限、别名。改后需重启" },
    { id: "config.yml", name: "config.yml", isLeaf: true, hoverText: "主配置：托管方式、性能、功能开关" },
  ]}
/>

<div style={{ marginBottom: "32px" }}></div>

目前只需关注 `resources/` 文件夹：所有自定义物品、方块和家具都存放在这里。

## 什么是“包”

CraftEngine 以“包”为单位组织自定义内容。每个包对应 `resources/` 下的一个文件夹，其中包含：

- **`configuration/`** —— 放 `.yml` 配置文件。物品怎么定义、方块什么属性，全写在这里
- **`resourcepack/`** —— 放模型文件（`.json`）、贴图文件（`.png`）、音效文件（`.ogg`）。结构和原版 Minecraft 资源包一样
- **`pack.yml`** —— 包的基本信息，包括名称、作者和命名空间

> 💡 文件夹名以 `.` 开头（比如 `.hidden_pack`）会被 CraftEngine 忽略，不会被加载。

你可以创建多个包（如 `tutorial`、`my_weapons`、`furniture_pack`），CraftEngine 会合并加载其中的内容。本教程始终使用同一个 `tutorial` 包。

## 什么是命名空间

命名空间就像内容 ID 的“姓氏”。Minecraft 中的物品、方块和贴图都有唯一 ID，例如 `minecraft:diamond`：`minecraft` 是命名空间，`diamond` 是名称。

自定义内容需要使用独立的命名空间，以免与原版内容重名。本教程使用 `tutorial`，因此物品 ID 会采用 `tutorial:xxx` 的格式。

命名空间只能用小写字母、数字、`-`、`_`、`.`、`/`。**不能有大写字母、不能有空格**。

💡 **小测验：以下哪些 ID 合法？**

1. `MyFirst:golden_sword`
2. `minecraft:steel furnace`
3. `abcd-efgh:1122.3344`
4. `craftengine:happy$crafting`
5. `test:tutorial_book`

<Details summary="点击看答案">

1. ❌ 大写字母不合法
2. ❌ 空格不合法
3. ✅ 数字、连字符、点号都合法
4. ❌ `$` 不合法
5. ✅ 小写字母和下划线都合法

</Details>

## 第 1 步：用命令创建包

无需手动创建文件夹。CraftEngine 可以通过一条命令生成完整的包结构。

在游戏里输入：

```
/ce resource create tutorial
```

这条命令会：

- 将 `tutorial` 设为包名和文件夹名，并默认用作命名空间
- 自动生成 `pack.yml`（写好了命名空间 `tutorial`、作者、版本等信息）
- 自动创建 `configuration/` 文件夹和 `resourcepack/` 下的全套子目录

命令执行成功后，打开 `plugins/CraftEngine/resources/tutorial/`，可以看到自动创建的全部子目录：

<PluginFileTree
  title="tutorial"
  initialTreeData={[
    {
      id: "configuration", name: "configuration", hoverText: "存放 .yml 配置文件，创建后默认为空",
      children: []
    },
    {
      id: "resourcepack", name: "resourcepack", hoverText: "放模型和贴图，结构和原版资源包一样",
      children: [
        {
          id: "assets", name: "assets",
          children: [
            {
              id: "tutorial", name: "tutorial", hoverText: "你的命名空间文件夹",
              children: [
                {
                  id: "models", name: "models",
                  children: [
                    { id: "models-block", name: "block", children: [] },
                    { id: "models-item", name: "item", children: [] },
                  ]
                },
                { id: "sounds", name: "sounds", children: [] },
                {
                  id: "textures", name: "textures",
                  children: [
                    { id: "textures-block", name: "block", children: [] },
                    { id: "textures-font", name: "font", children: [] },
                    { id: "textures-gui", name: "gui", children: [] },
                    { id: "textures-item", name: "item", children: [] },
                  ]
                },
              ]
            }
          ]
        }
      ]
    },
    { id: "pack.yml", name: "pack.yml", isLeaf: true, hoverText: "自动生成的元数据：命名空间、作者、版本" },
  ]}
/>

<div style={{ marginBottom: "32px" }}></div>

> ⚠️ 如果提示 `pack already exists`（包已存在），说明 `resources/tutorial/` 已存在。请改用其他包名，或确认不再需要该文件夹后将其删除，再重新执行命令。

> 💡 `pack.yml` 中已经自动填好命名空间、作者和版本信息，通常无需修改。

## 第 2 步：创建配置文件

包创建完成后，在 `configuration/` 中新建第一个配置文件。下一章将用它来定义物品。

### 用什么编辑器

请使用**纯文本编辑器**。推荐 [VS Code](https://code.visualstudio.com/)：它免费且支持语法高亮，有助于减少格式错误。

### 方式一：文件管理器

打开 `configuration` 文件夹，**右键 → 新建 → 文本文档**，把文件名改成 `items.yml`。

> ⚠️ **文件名必须是 `items.yml`，不是 `items.yml.txt`。** Windows 默认隐藏文件后缀——你可能以为改成了 `.yml`，实际是 `.yml.txt`。检查方法：文件管理器顶部点「查看」→ 勾选「文件扩展名」，确认后缀确实是 `.yml`。

### 方式二：命令行

```bash
# 先进到 configuration 目录
cd plugins/CraftEngine/resources/tutorial/configuration

# Linux / Mac
touch items.yml

# Windows PowerShell
ni items.yml
```

创建完成后，使用 VS Code、记事本或 `nano` 等纯文本编辑器打开文件，即可开始编写配置。

## 第 3 步：了解配置文件的加载方式

所有 `.yml` 和 `.json` 文件放在 `configuration/` 下就会被加载。你可以把配置写在**一个文件**里，也可以**拆成多个文件**——CraftEngine 会把它们全读进去合并。

比如你可以按类型分：

<PluginFileTree
  title="configuration"
  initialTreeData={[
    { id: "items.yml", name: "items.yml", isLeaf: true, hoverText: "所有物品" },
    { id: "blocks.yml", name: "blocks.yml", isLeaf: true, hoverText: "所有方块" },
    { id: "furniture.yml", name: "furniture.yml", isLeaf: true, hoverText: "所有家具" },
  ]}
/>

<div style={{ marginBottom: "32px" }}></div>

也可以按功能分：

<PluginFileTree
  title="configuration"
  initialTreeData={[
    { id: "weapons.yml", name: "weapons.yml", isLeaf: true, hoverText: "武器类物品" },
    { id: "materials.yml", name: "materials.yml", isLeaf: true, hoverText: "材料类物品" },
    { id: "decorations.yml", name: "decorations.yml", isLeaf: true, hoverText: "装饰方块" },
  ]}
/>

<div style={{ marginBottom: "32px" }}></div>

两种分类方式都可以。本教程为便于操作，先将物品配置集中写在 `items.yml` 中；制作方块和家具时再创建新文件。

> 💡 `configuration/` 下也可以建子文件夹来分类管理。比如 `configuration/weapons/`、`configuration/blocks/`——CraftEngine 会递归扫描所有子目录，里面的 `.yml` 和 `.json` 全都会被加载。

## 让修改后的配置生效

每次修改配置文件（`.yml` 或 `.json`）后，都需要让 CraftEngine 重新加载。请根据改动类型选择命令：

| 修改内容 | 运行命令 |
|:---|:---|
| 改了 `.yml` 配置（加物品、改名字、改属性） | `/ce reload config` |
| 改了合成配方 | `/ce reload recipe` |
| 改了贴图文件（`.png`）或模型文件（`.json`） | `/ce reload all` |
| 不确定该用哪个 | `/ce reload all`（最稳妥） |

> ⚠️ **请特别注意**：修改贴图后，仅运行 `/ce reload config` 不会生效。贴图变更必须通过 `/ce reload all` 重建资源包。遇到“贴图已经修改，但游戏内没有变化”的情况时，请先检查是否运行了正确的重载命令。

直接运行 `/ce reload`（不带参数）等同于 `/ce reload config`。

---
title: 🔮 Skript
id: skript
---

import SkriptCard from '@site/src/components/SkriptCard';

:::tip
高级 Skript 用户可以通过反射实现更复杂的功能。如果在使用 CraftEngine 提供的 Skript 功能时遇到问题，请[提交反馈](https://github.com/Xiao-MoMi/craft-engine/issues/new)。如果希望添加新的 Skript 功能并熟悉 Java，也欢迎通过拉取请求贡献代码。
:::

:::warning
你需要安装 [Skript](https://modrinth.com/plugin/skript) 2.15.0 或更高版本。
:::

## 类型

CraftEngine 额外注册了两种可存入变量的类型。可通过 `parsed as` 从字符串解析：

```applescript
# 完整的不可变方块状态（ID + 属性）
set {_state} to "default:table_lamp[lit=true,facing=east]" parsed as custom block state

# 方块状态匹配器，主要用于事件过滤器
set {_matcher} to "default:table_lamp[lit=true]" parsed as unsafe block state matcher
```

在效果和事件过滤器中也可以直接使用不带引号的字面量，例如 `place custom block default:table_lamp[lit=true] at player`。


## 事件

<SkriptCard
  title="放置或破坏 CraftEngine 方块事件"
  type="Event"
  description="当放置和破坏 CraftEngine 方块时触发"
  syntax={[
    '[on] (break[ing]|1¦min(e|ing)) of (custom|ce|craft-engine) block[s] [[of] %-unsafeblockstatematchers%]',
    '[on] (plac(e|ing)|build[ing]) of (custom|ce|craft-engine) block[s] [[of] %-unsafeblockstatematchers%]'
  ]}
  values={[
    'event-block - 事件方块',
    'event-location - 事件位置',
    'event-player - 事件玩家',
    'event-world - 事件世界'
  ]}
  examples={[
    `on break of ce block default:palm_log[axis=y]:
      send "你破坏了自定义方块！" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="放置或破坏 CraftEngine 家具事件"
  type="Event"
  description="当放置和破坏 CraftEngine 家具时触发"
  syntax={[
    '[on] (break[ing]) of [(custom|ce|craft-engine)] furniture[s] [[of] %-strings%]',
    '[on] (plac(e|ing)|build[ing]) of [(custom|ce|craft-engine)] furniture[s] [[of] %-strings%]'
  ]}
  values={[
    'event-entity - 事件实体',
    'event-location - 事件位置',
    'event-player - 事件玩家',
    'event-world - 事件世界'
  ]}
  examples={[
    `on place of custom furniture default:chinese_lantern:
      send "你放置了自定义家具！" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="点击 CraftEngine 方块或家具事件"
  type="Event"
  description="当点击 CraftEngine 方块和家具时触发"
  syntax={[
    '[on] [(" + RIGHT + ":right|" + LEFT + ":left)(| |-)][mouse(| |-)]click[ing] of (ce|craft-engine) [on %-unsafeblockstatematchers/strings%] [(with|using|holding) %-itemtype%]',
    '[on] [(" + RIGHT + ":right|" + LEFT + ":left)(| |-)][mouse(| |-)]click[ing] of (ce|craft-engine) (with|using|holding) %itemtype% on %unsafeblockstatematchers/strings%'
  ]}
  values={[
    'event-block - 事件方块',
    'event-entity - 事件实体',
    'event-location - 事件位置',
    'event-player - 事件玩家',
    'event-world - 事件世界'
  ]}
  examples={[
    `on right-click of craft-engine on default:chinese_lantern:
       send "你点击了自定义方块！" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 重新加载事件"
  type="Event"
  description="当 CraftEngine 加载时触发"
  syntax={[
    '[on] (ce|craft(engine|-engine)) [first] (load[ed]|reload)'
  ]}
  examples={[
    `on craft-engine loaded:
      send "嘿，CraftEngine 已加载，现在可以获取 CE 物品了！" to console`,
    `on craft-engine first loaded:
      send "嘿，CraftEngine 已在服务器启用时加载，脚本现在将初始化一些内容！" to console`,
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="击打 CraftEngine 家具事件"
  type="Event"
  description="当玩家击打 CraftEngine 家具时触发，可取消"
  syntax={[
    '[on] hit[ting] [of] [(custom|ce|craft-engine)] furniture[s] [[of] %-strings%]'
  ]}
  values={[
    'event-entity - 事件实体',
    'event-location - 事件位置',
    'event-player - 事件玩家',
    'event-world - 事件世界'
  ]}
  examples={[
    `on hit of furniture "default:wooden_chair":
      cancel event`
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="尝试放置 CraftEngine 方块或家具事件"
  type="Event"
  description="当玩家尝试放置自定义方块或家具时触发（先于实际放置），可取消"
  syntax={[
    '[on] attempt[ing] to place (custom|ce|craft-engine) block[s] [[of] %-unsafeblockstatematchers%]',
    '[on] attempt[ing] to place [(custom|ce|craft-engine)] furniture[s] [[of] %-strings%]'
  ]}
  values={[
    'event-block - 事件方块',
    'event-location - 事件位置',
    'event-player - 事件玩家',
    'event-world - 事件世界'
  ]}
  examples={[
    `on attempt to place custom block:
      cancel event`
  ]}
  defaultOpen={false}
  i18n={{
    label: "事件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

## 条件

<SkriptCard
  title="是 CraftEngine 物品"
  type="Condition"
  description="检查物品是否为来自 CraftEngine 的自定义物品"
  syntax={[
    '%itemstack/itemtype/slot% (is [a[n]]|are) (custom|ce|craft-engine) item[s]',
    '%itemstack/itemtype/slot% (isn\'t|is not|aren\'t|are not) [a[n]] (custom|ce|craft-engine) item[s]'
  ]}
  examples={[
    `on mine:
      if event-player's tool is a custom item:
        send "你正在使用自定义物品挖掘" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "条件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="是 CraftEngine 方块"
  type="Condition"
  description="检查方块是否为来自 CraftEngine 的自定义方块"
  syntax={[
    '%blocks% (is|are) [a[n]] (custom|ce|craft-engine) block[s]',
    '%blocks% (is|are) (n\'t| not) [a[n]] (custom|ce|craft-engine) block[s]'
  ]}
  examples={[
    `on mine:
      if event-block is custom block:
        send "你破坏了一个自定义方块" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "条件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="是 CraftEngine 家具"
  type="Condition"
  description="检查实体是否为来自 CraftEngine 的自定义家具"
  syntax={[
    '%entities% (is|are) [a[n]] [(custom|ce|craft-engine)] furniture[s]',
    '%entities% (is|are) (n\'t| not) [a[n]] [(custom|ce|craft-engine)] furniture[s]'
  ]}
  examples={[
    `on click:
      set {_entity} to nearest pig
      if {_entity} is a ce furniture:
        send "我在做什么？" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "条件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="是 CraftEngine 家具座位"
  type="Condition"
  description="检查实体是否为 CraftEngine 家具的座位"
  syntax={[
    '%entities% (is|are) [a[n]] [(custom|ce|craft-engine)] furniture seat[s]',
    '%entities% (is|are) (n\'t| not) [a[n]] [(custom|ce|craft-engine)] furniture seat[s]'
  ]}
  examples={[
    `if target entity is a furniture seat:
      send "这是一个座位！" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "条件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="是 CraftEngine 家具碰撞体"
  type="Condition"
  description="检查实体是否为 CraftEngine 家具的碰撞实体"
  syntax={[
    '%entities% (is|are) [a[n]] [(custom|ce|craft-engine)] furniture (collider|collision entit(y|ies))',
    '%entities% (is|are) (n\'t| not) [a[n]] [(custom|ce|craft-engine)] furniture (collider|collision entit(y|ies))'
  ]}
  examples={[
    `if target entity is a furniture collider:
      send "这是一个碰撞体！" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "条件",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

## 表达式

<SkriptCard
  title="CraftEngine 物品"
  type="Expression"
  description="通过命名空间 ID 获取自定义物品"
  syntax={[
    '[(the|a)] (custom|ce|craft-engine) item [with [namespace] id] %strings%',
  ]}
  examples={[
    `set {_item} to custom item with namespace id "default:topaz"`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 物品命名空间 ID"
  type="Expression"
  description="获取自定义物品的命名空间 ID"
  syntax={[
    '(custom|ce|craft-engine) item [namespace] id of %itemstack/itemtype/slot%',
    '%itemstack/itemtype/slot%\'[s] (custom|ce|craft-engine) item [namespace] id'
  ]}
  examples={[
    `set {_itemId} to craft-engine item id of player's tool`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 方块命名空间 ID"
  type="Expression"
  description="获取自定义方块的命名空间 ID"
  syntax={[
    '%blocks/blockdata/customblockstates%\'s (custom|ce|craft-engine) block [namespace] id',
    '(custom|ce|craft-engine) block [namespace] id of %blocks/blockdata/customblockstates%'
  ]}
  examples={[
    `on mine:
      if event-block's ce block id is "default:chinese_lantern":
        send "破坏了一个中式灯笼方块" to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 方块状态"
  type="Expression"
  description="获取自定义方块的方块状态"
  syntax={[
    '%blocks/blockdata/customblockstates%\'s (custom|ce|craft-engine) block[ ]state',
    '(custom|ce|craft-engine) block[ ]state of %blocks/blockdata/customblockstates%'
  ]}
  examples={[
    `on mine:
      send event-block's ce block state to event-player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 家具命名空间 ID"
  type="Expression"
  description="从实体获取家具的命名空间 ID"
  syntax={[
    '%entities%\'s [(custom|ce|craft-engine)] furniture [namespace] id',
  ]}
  examples={[
    `set {_furnitureId} to {_entity}'s craft-engine furniture id`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 方块状态属性"
  type="Expression"
  description="获取或设置自定义方块状态的单个属性。方块状态是不可变的：设置属性会替换变量中存储的状态"
  syntax={[
    '(custom|ce|craft-engine) block [state] propert(y|ies) %strings% of %customblockstates%',
    '%customblockstates%\'[s] (custom|ce|craft-engine) block [state] propert(y|ies) %strings%'
  ]}
  examples={[
    `set {_s} to "default:table_lamp[lit=true]" parsed as custom block state
      set custom block property "facing" of {_s} to "south"
      place custom block {_s} at target block`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 方块全部状态"
  type="Expression"
  description="获取自定义方块的所有可能状态"
  syntax={[
    '[(all|the)] [possible] [block] states of (custom|ce|craft-engine) block[s] %strings%',
  ]}
  examples={[
    `loop all states of custom block "default:table_lamp":
      send "%loop-value%"`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 家具变体"
  type="Expression"
  description="获取、设置或重置家具实体的变体。重置会恢复默认变体"
  syntax={[
    '(custom|ce|craft-engine) furniture variant of %entities%',
    '%entities%\'[s] (custom|ce|craft-engine) furniture variant'
  ]}
  examples={[
    `set furniture variant of target entity to "wall"`,
    `reset furniture variant of target entity`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="CraftEngine 家具全部变体"
  type="Expression"
  description="获取家具定义的所有变体名称"
  syntax={[
    '[(all|the)] variants of [(custom|ce|craft-engine)] furniture %strings%',
  ]}
  examples={[
    `send variants of furniture "default:flower_basket" to player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "表达式",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

## 效果

<SkriptCard
  title="放置 CraftEngine 方块"
  type="Effect"
  description="放置一个自定义方块"
  syntax={[
    'place (custom|ce|craft-engine) block %customblockstates% [at] [%directions% %locations%]',
  ]}
  examples={[
    `place custom block default:palm_log[axis=x] at location of the player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "效果",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="放置 CraftEngine 家具"
  type="Effect"
  description="放置一个家具，可指定变体"
  syntax={[
    'place [(custom|ce|craft-engine)] furniture[s] %strings% [with variant %-strings%] [at] [%directions% %locations%]',
  ]}
  examples={[
    `place furniture "default:bench" at location of player`,
    `place furniture "default:flower_basket" with variant "wall" at location of player`
  ]}
  defaultOpen={false}
  i18n={{
    label: "效果",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="移除 CraftEngine 方块"
  type="Effect"
  description="移除指定位置的自定义方块，非自定义方块会被忽略"
  syntax={[
    '(remove|break) (custom|ce|craft-engine) block[s] [at] [%directions% %locations%]',
  ]}
  examples={[
    `remove custom block at target block`
  ]}
  defaultOpen={false}
  i18n={{
    label: "效果",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="移除 CraftEngine 家具"
  type="Effect"
  description="移除一个家具"
  syntax={[
    'remove [(custom|ce|craft-engine)] furniture %entities%',
  ]}
  examples={[
    `remove craft-engine furniture target entity`
  ]}
  defaultOpen={false}
  i18n={{
    label: "效果",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

<SkriptCard
  title="强制设置 CraftEngine 家具变体"
  type="Effect"
  description="强制设置家具实体的变体，跳过碰撞检查。与遵循碰撞检查的 'set furniture variant of ... to ...' 相对"
  syntax={[
    'force[fully] set [(custom|ce|craft-engine)] furniture variant of %entities% to %strings%',
  ]}
  examples={[
    `force set furniture variant of target entity to "ceiling"`
  ]}
  defaultOpen={false}
  i18n={{
    label: "效果",
    syntax: "语法格式:",
    field: "事件字段:",
    example: "示例:",
  }}
/>

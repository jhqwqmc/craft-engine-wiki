---
title: ∅ 空模型
id: empty
---

## 概述

不渲染任何物品模型。适合作为回落映射，当不需要显示物品模型时使用。

```yaml
items:
  demo:hidden_item:
    material: paper
    model:
      type: minecraft:empty
```

`transformation` **不**支持于 `empty`。

---
title: 🫑 方块实体
id: entities
---

import Highlight from '@site/src/components/Highlight';

## 简介

方块实体结合了方块与实体的特性。它占据世界中的一个方块位置，但能像实体一样**存储数据、执行更新并支持自定义渲染**。通过**方块实体接口**，你可以为方块赋予更高级功能，例如：

- 基础电容库：根据当前存储的能量动态更改方块外观
- 斯特林发电机：基于游戏刻运行，定期燃烧燃料发电

在现有的内置方块实体类型库持续扩充的同时，CraftEngine 将在未来的版本中通过引入更多功能多样、开箱即用的组件来显著扩展这些功能配置。

## 基于实体的渲染

### 它是如何工作的？

众所周知，单个方块的变体数量是有限的。以绊线为例，其最多只有 128 种变体。这意味着在不破坏绊线原本功能的前提下，最多只能释放 127 种自定义方块状态。然而，通过移除其中一种状态的纹理使其完全透明，并在方块中心放置展示实体，便能实现近乎无限的自定义方块。

:::warning

需要注意的是，某些原版方块使用了硬编码的渲染器。当方块使用的模型尺寸与完整方块不匹配时，可能会出现透视方块效果，如下图所示。

<img src={require('/img/block_entities_1.png').default} alt="" style={{ width: '100%', height: 'auto', maxWidth: '128px', maxHeight: '128px', objectFit: 'contain' }} />

:::

要配置展示实体，在 `state` 或 `appearances` 外观中添加 `entity_renderer` 选项即可。

## 基本用法

`entity_renderer` 接受一个对象或对象列表。每个对象描述一个渲染元素，多个元素可叠加组合。

```yaml
# 单个渲染元素（对象写法）
entity_renderer:
  type: item_display
  item: default:sofa

# 多个渲染元素（列表写法）
entity_renderer:
  - type: item_display
    item: default:sofa
  - type: text_display
    text: 沙发
```

:::info
类型自动推断

如果未指定 `type`，插件会根据其他字段自动推断：
- 包含 `text` → `text_display`
- 包含 `item` → `item_display`

因此最简单的写法可以省略 `type`：

```yaml
entity_renderer:
  item: default:sofa     # 自动推断为 item_display
```
:::

## 显示参数

以下参数适用于 `item_display` 和 `text_display` 两种展示实体类型。

<details>
<summary>展开显示参数列表</summary>

```yaml
# 位置与变换
translation: 0,0.5,0       # 偏移量 (x,y,z)，推荐使用此方式调整位置
position: 0.5,0.5,0.5      # 绝对位置 (x,y,z)，默认方块中心
scale: 1,1,1               # 缩放 (x,y,z)，也支持单个数字如 scale: 1.5

# 旋转（三选一，推荐 rotation）
yaw: 0                     # 绕 Y 轴
pitch: 0                   # 绕 X 轴
rotation: 45               # 单数字 → 绕 Y 轴
rotation: 45,45,0          # 欧拉角 (x,y,z)
rotation: 0,0,0.707,0.707  # 四元数 (x,y,z,w)

# 显示
billboard: fixed           # fixed / vertical / horizontal / center
                           # 固定  / 固定垂直轴 / 固定水平轴 / 始终面向玩家
glow_color: 255,255,255    # 发光颜色 (RGB)
brightness:                # 亮度覆写
  block_light: 15
  sky_light: 15
view_range: 1.0            # 可视距离系数

# 阴影
shadow_radius: 0           # 阴影半径
shadow_strength: 1         # 阴影强度
```

</details>

:::tip
建议使用 `translation` 代替 `position`，使用 `rotation` 代替 `yaw`/`pitch`。这样插件可以在不发送位置同步包的情况下更新渲染，保证最佳视觉效果。
:::

## 渲染元素类型

### item_display（物品展示实体）

基于原版物品展示实体渲染自定义物品模型。**当未指定 `type` 且包含 `item` 时，自动推断为此类型。**

**必填：**

```yaml
type: item_display
item: default:sofa          # 物品 ID
```

**可选：**

```yaml
display_transform: none     # 物品变换模式：
                            # none / third_person_left_hand / third_person_right_hand
                            # first_person_left_hand / first_person_right_hand
                            # head / gui / ground / fixed
```

`display_transform`（也可写作 `display_context`）决定物品模型的变换方式。大多数情况下保持默认 `none` 即可；如需模拟手持、地面等视角的物品外观，可指定对应模式。

<details>
<summary>完整示例</summary>

```yaml
entity_renderer:
  type: item_display
  item: default:sofa
  translation: 0,0.5,0
  scale: 1,1,1
  rotation: 0,45,0
  billboard: fixed
  glow_color: 255,200,100
  brightness:
    block_light: 15
    sky_light: 15
  view_range: 1.0
  shadow_radius: 0
  shadow_strength: 1
  display_transform: none
  tint_source:              # 颜色提供器（详见下方）
    type: default
    components:
      - minecraft:dyed_color
```

</details>

### text_display（文本展示实体）

基于原版文本展示实体渲染文字。**当未指定 `type` 且包含 `text` 时，自动推断为此类型。**

**必填：**

```yaml
type: text_display
text: 你好 <papi:player_name>      # 文本内容，支持 MiniMessage 和 PAPI 占位符
```

**可选：**

```yaml
line_width: 200                    # 行宽（像素）
background_color: 64,0,0,0         # 背景色 (ARGB)，默认 0x40000000（半透明黑）
text_opacity: -1                   # 文字不透明度 (0-255)，-1 = 默认
has_shadow: false                  # 是否有文字阴影
is_see_through: false              # 是否可穿透看到文字背面
use_default_background_color: false # 是否使用默认背景色
alignment: center                  # 对齐方式：center / left / right
```

<details>
<summary>完整示例</summary>

```yaml
entity_renderer:
  type: text_display
  text: <yellow>点击交互</yellow>
  translation: 0,1.2,0
  scale: 1.5
  billboard: center
  alignment: center
  line_width: 200
  background_color: 0,0,0,0        # ARGB，全透明背景
  text_opacity: 200
  has_shadow: true
  is_see_through: false
  view_range: 0.8
```

</details>

### item（掉落物）

渲染一个旋转的掉落物实体，效果类似地面上的物品。

```yaml
type: item
item: default:sleeper_sofa         # 必填：物品 ID
position: 0.5,0.5,0.5              # 可选：位置 (x,y,z)，默认方块中心
```

掉落物仅支持 `position`，其余显示参数均不适用。掉落物会自动带有旋转动画，适合简单的物品展示场景。

<details>
<summary>带颜色提供器的示例</summary>

```yaml
entity_renderer:
  type: item
  item: default:colored_block
  position: 0.5
  tint_source:
    type: default
    components:
      - minecraft:dyed_color
```

</details>

### armor_stand（盔甲架）

基于盔甲架渲染物品模型。盔甲架支持装备头盔物品并将其渲染在正确的位置。

```yaml
type: armor_stand
item: default:bench                 # 必填：物品 ID
position: 0,0.2,0                   # 可选：位置 (x,y,z)，默认方块中心
yaw: 0                              # 可选：绕 Y 轴旋转
pitch: 0                            # 可选：绕 X 轴旋转
scale: 1                            # 可选：缩放，默认 1
small: false                        # 可选：是否小盔甲架，默认 false
glow_color: white                   # 可选：发光颜色（支持旧版颜色名）
```

`glow_color` 支持的颜色名：`black, dark_blue, dark_green, dark_aqua, dark_red, dark_purple, gold, gray, dark_gray, blue, green, aqua, red, light_purple, yellow, white`。

### better_model（BetterModel 模型）

:::info
需要安装 BetterModel 插件
:::

渲染 BetterModel 插件中的自定义模型。

```yaml
type: better_model
model: custom_model                 # 必填：模型名称
position: 0.5,0.5,0.5               # 可选：位置 (x,y,z)，默认方块中心
yaw: 0                              # 可选：绕 Y 轴旋转
pitch: 0                            # 可选：绕 X 轴旋转
sight_trace: true                   # 可选：是否参与射线追踪，默认 true
```

### model_engine（ModelEngine 模型）

:::info
需要安装 ModelEngine 插件
:::

渲染 ModelEngine 插件中的自定义模型。

```yaml
type: model_engine
model: custom_model                 # 必填：模型名称
position: 0.5,0.5,0.5               # 可选：位置 (x,y,z)，默认方块中心
yaw: 0                              # 可选：绕 Y 轴旋转
pitch: 0                            # 可选：绕 X 轴旋转
```

## 颜色提供器（tint_source）

颜色提供器用于为方块实体渲染元素的物品**动态着色**。它从方块行为（如 `tint_source_block` 或 `display_item_block`）中读取颜色数据，并应用到渲染的物品上。

### 简单用法

大多数情况下只需一行：

```yaml
entity_renderer:
  type: item_display
  item: default:sofa
  tint_source: minecraft:dyed_color       # 字符串简写
```

等价于：

```yaml
entity_renderer:
  type: item_display
  item: default:sofa
  tint_source:
    type: default
    components:
      - minecraft:dyed_color
    index: 0                              # 颜色提供器索引，默认 0
```

### 多元素着色

当一个方块有多个渲染元素时，每个元素可以绑定不同的着色索引。索引对应方块行为中注册颜色提供器的顺序：

```yaml
entity_renderer:
  - item: example:element_1
    tint_source:
      type: default
      components:
        - minecraft:dyed_color
      index: 0                            # 对应第 1 个颜色提供器
  - item: example:element_2
    tint_source:
      type: default
      components:
        - minecraft:dyed_color
      index: 1                            # 对应第 2 个颜色提供器
  - item: example:element_3
    tint_source:
      type: default
      components:
        - minecraft:dyed_color
      index: 2                            # 对应第 3 个颜色提供器
```

`components` 列表指定需要应用颜色的物品组件，常见的有 `minecraft:dyed_color`（染色）、`minecraft:custom_model_data` 等。一个 `tint_source` 可同时作用于多个组件。

![](/img/block_tint_source.png)

## 实体剔除（entity_culling）

实体剔除控制方块实体在客户端侧的可见性管理，用于优化性能。

**禁用剔除**（实体始终可见）：

```yaml
entity_renderer: ...
entity_culling: false
```

**启用剔除**（默认行为，可自定义参数）：

```yaml
entity_renderer: ...
entity_culling:
  aabb: -0.5,-0.5,-0.5,0.5,1.5,0.5     # 剔除用碰撞箱 (minX,minY,minZ,maxX,maxY,maxZ)
                                         # 默认值覆盖整个方块
  view_distance: 64                      # 最大可视距离（格），-1 表示无限
  aabb_expansion: 0.5                    # 碰撞箱扩展量，默认 0.5
  ray_tracing: true                      # 是否启用射线追踪剔除
```

参数说明：

| 参数 | 类型 | 默认值 | 说明 |
|:---|:---|:---|:---|
| `aabb` | 6 个数字 | 方块范围 | 自定义剔除碰撞箱，模型体积大时需要调大 |
| `view_distance` | 整数 | 由 config.yml 决定 | 超出此距离的实体不再渲染。不建议设为 -1 同时开启 `ray_tracing` |
| `aabb_expansion` | 数字 | 0.5 | 碰撞箱扩展量，影响射线追踪的判定范围 |
| `ray_tracing` | 布尔 | true | 是否通过射线追踪判断实体是否被遮挡 |

## 条件可见（conditions）

每个渲染元素可以配置 `conditions`，使该元素仅对满足条件的玩家可见。

```yaml
entity_renderer:
  - type: item_display
    item: default:secret_block
    conditions:
      - type: permission
        permission: "vip.secret"
      - type: world
        world: "world_nether"
```

条件之间是**与**（AND）关系——全部满足才可见。支持的条件类型参见条件系统文档。

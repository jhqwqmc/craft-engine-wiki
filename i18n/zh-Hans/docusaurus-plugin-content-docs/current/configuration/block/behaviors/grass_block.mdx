---
title: 🌿 草方块
id: grass_block
---

**草方块**允许你对其使用骨粉来生成草和花。请确保为你的方块添加 `minecraft:dirt` 和 `minecraft:supports_vegetation` 标签，否则草方块无法在其上存活。

```yml
blocks:
  default:grass_block:
    behavior:
      type: grass_block
      feature: minecraft:grass_bonemeal
```

![](/img/grass_block.png)
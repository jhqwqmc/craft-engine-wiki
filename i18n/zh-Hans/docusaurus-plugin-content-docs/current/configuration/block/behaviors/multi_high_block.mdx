---
title: 🏢 多层方块
id: multi_high_block
---

| 属性名称 | 属性类型 | 是否必需 |
|------|------|------|
| high | int  | 是    |

**多层方块**根据 `high` 属性范围差决定方块高度。

```yaml
blocks:
  default:multi_high_block:
    states:
      properties:
        high:
          type: int
          default: 0
          range: 0~2
    behavior:
      type: multi_high_block
```
